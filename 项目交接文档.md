# 「记录我的中国行」项目交接文档

> 本文档完整记录项目代码结构、各部分功能、所有修改过程及 GitHub 推送全流程，供交接给其他 Agent 使用。

> **当前基线提示（2026-08-15）**：项目已经接入 Supabase 账号与阿里云 OSS 跨设备照片同步。下方“〇、当前生产环境交接”是最新权威说明；后面的旧章节保留为历史记录，如有冲突，以“〇”章节为准。

> **工作目录提示（2026-08-15 下午）**：最新代码在 `F:\trae\Your-China-Travel`（index.html 约 541KB，含 0.13 节全部 UI 迭代，**尚未 commit / push**）。`F:\codex\codex-list\projects\Your-China-Travel` 是旧快照（index.html 约 469KB，对应已部署版本）。两份副本已分叉，后续提交请以 `F:\trae\Your-China-Travel` 为准。

> **最新迭代提示（2026-08-16）**：`F:\trae\Your-China-Travel\index.html` 已更新至约 574KB，包含 0.16 节全部功能（卡片位置锁定、连线定长、深浅主题切换、浅色主题配色体系、多处字体/按钮颜色调整），**仍未 commit / push**。

> **最新迭代提示（2026-08-18）**：`F:\trae\Your-China-Travel\index.html` 已更新至约 698KB，包含 0.18 节全部功能（个人资料头像/昵称跨设备同步、霓虹发光按钮、省份字体主题切换修复、城市悬停光晕收紧、搜索高亮闪烁 6 次、切账号本地数据合并同步、双击省份定位放大、我的家乡独立入口），本次随 index.html 一并提交推送。

---

## 〇、当前生产环境交接（2026-08-15，权威基线）

### 0.1 当前完成状态

- 正式网站：`https://zwxll.github.io/Your-China-Travel/`
- GitHub 仓库：`https://github.com/zwxll/Your-China-Travel.git`
- 当前工作目录：`F:\codex\codex-list\projects\Your-China-Travel`
- 账号系统：Supabase Auth，支持用户自行注册、登录、退出及跨设备登录。
- 旅行数据：Supabase PostgreSQL 表 `public.travel_snapshots`，每个用户一行快照。
- 照片文件：阿里云 OSS 私有 Bucket `your-china-travel-8877-hk`，地域为中国香港 `oss-cn-hongkong`。
- 本机缓存：IndexedDB 数据库 `TravelPhotosDB`，对象仓库为 `photos` 与 `cityMeta`。
- OSS 签名服务：Supabase Edge Function `oss-media`。
- 云同步安全基线提交：`9ee1b61 fix: 防止恢复期间覆盖或删除云端照片`。
- 省份视图：34 个省级区域保持常亮，全部已点亮城市显示封面信息卡与动态弯曲连线；卡片自动避让，并在地图拖动、缩放及左右面板收展时实时重排。

### 0.2 当前架构与数据流

```text
浏览器
  ├─ Supabase Auth：注册、登录、会话
  ├─ IndexedDB：当前设备缓存照片与城市资料
  ├─ travel_snapshots：保存照片路径、城市、顺序、元数据
  └─ 调用 oss-media Edge Function
       ├─ 验证 Supabase 登录令牌
       ├─ 验证路径必须属于 users/<当前用户ID>/
       └─ 返回 5 分钟有效的 OSS PUT / GET 临时签名地址
            └─ 私有阿里云 OSS：保存实际图片文件
```

未登录时，数据只保存在当前浏览器 IndexedDB。登录后，页面自动把旅行清单同步到 Supabase，把实际照片上传到 OSS；换设备登录同一账号后，页面自动恢复云端内容。IndexedDB 登录后仍保留为本机缓存，不是唯一存储。

### 0.3 用户权限语义

- 所有人都可以在网站注册账号。
- 已登录用户可以新增、查看、删除**自己账号**的照片。
- 用户 A 不能读取或删除用户 B 的照片。
- OSS 必须保持“私有”，不要设置成“公共读写”。公共读写代表绕过网站登录即可直接操作 OSS，不等于“允许所有登录用户使用”。
- 照片路径格式：`users/<Supabase user id>/photos/<uuid>.<ext>`。
- `oss-media` 的 `ownsPath()` 会在签名和删除前校验当前用户 ID，RAM 策略只负责限制专用 RAM 用户可以操作的 Bucket 范围。

### 0.4 云端配置清单

| 项目 | 当前值 / 要求 |
|------|---------------|
| Supabase Project Ref | `zpqbbawremufcxxszgde` |
| Supabase 数据表 | `public.travel_snapshots`，已启用 RLS |
| Edge Function | `oss-media` |
| Verify JWT with legacy secret | **关闭**；函数内部仍调用 `auth.getUser(token)` 验证登录用户 |
| OSS Bucket | `your-china-travel-8877-hk` |
| OSS Region | `oss-cn-hongkong` / `cn-hongkong`（SDK 使用配置中的 Region） |
| OSS ACL | **私有** |
| 正式 CORS Origin | `https://zwxll.github.io` |
| 本地 CORS Origin | `http://127.0.0.1:8765` |
| 临时签名有效期 | 300 秒 |

Supabase Edge Function Secrets 必须存在：

```text
ALIYUN_OSS_ACCESS_KEY_ID
ALIYUN_OSS_ACCESS_KEY_SECRET
ALIYUN_OSS_BUCKET
ALIYUN_OSS_REGION
```

AccessKey Secret、Supabase Secret key、service_role key 禁止写入 `index.html`、`supabase-config.js`、Markdown、HAR 或 Git 仓库。`supabase-config.js` 只能放浏览器可公开的 Project URL 与 Publishable key。

### 0.5 OSS CORS 与 RAM 权限

OSS CORS 至少包含：

- 来源：`https://zwxll.github.io`、`http://127.0.0.1:8765`
- Methods：`GET`、`PUT`、`DELETE`、`HEAD`
- Allowed Headers：`*`
- Expose Headers：`ETag`、`x-oss-request-id`
- Max Age：600 秒

专用 RAM 用户只授权当前 Bucket 的对象读取、上传和删除，不给控制台管理权限。当前自定义策略名为 `YourChinaTravelOSSAccess`。策略资源必须指向新的香港 Bucket，例如：

```text
acs:oss:*:*:your-china-travel-8877-hk/users/*
```

### 0.6 关键文件

```text
Your-China-Travel/
├── index.html                         # 主页面、IndexedDB、账号与同步逻辑
├── supabase-config.js                 # 浏览器端公开 Supabase 配置
├── supabase-setup.sql                 # travel_snapshots、RLS、旧 Supabase Storage 兼容配置
├── supabase/functions/oss-media/
│   └── index.ts                       # 登录验证、用户路径校验、OSS V4 签名与删除
├── README.md                          # 面向使用者的部署与配置说明
├── PROJECT_MEMORY.md                  # 面向后续 Agent 的快速记忆
└── 项目交接文档.md                    # 完整交接文档
```

`supabase-setup.sql` 中的 `travel-media` Supabase Storage Bucket 是旧方案兼容项；当前新增照片的主存储是阿里云 OSS。

### 0.7 同步安全逻辑

- 批量添加照片时，会等待所有 IndexedDB 写入完成后再生成云端快照。
- 云端恢复期间设置 `cloudSuppressDirty`，禁止把半恢复或空状态反向覆盖云端。
- 恢复前先下载并准备全部照片与元数据，全部成功后才清空本地缓存并写入。
- 恢复开始时会取消已排队的自动同步定时器。
- 同一账号刷新时，如果本机记录的云端更新时间与 `travel_snapshots.updated_at` 一致，并且 IndexedDB 含有快照需要的全部图片路径，则直接使用本机缓存，不调用 OSS 下载。
- 其他设备产生新快照时，恢复流程按 `storagePath` 复用 IndexedDB 已有图片，只向 OSS 下载本机缺少或路径发生变化的图片。
- 当前版本不再自动删除“未出现在快照中的 OSS 文件”，避免竞态条件误删照片；删除照片只走用户明确触发的删除流程。
- 首次登录空账号且本机有资料时，会迁移本机资料；云端与本机同时有资料时，应由用户选择保留哪一份。
- 不要连续点击“账号同步”、退出、重新登录；应等待顶部状态显示“已同步”。

### 0.8 生产验证流程

每次修改同步代码或 Edge Function 后按以下顺序验证：

1. 电脑登录测试账号，新增一张容易识别的测试图片。
2. 等顶部显示“已同步”，不要手工重复触发同步。
3. 在 OSS 控制台确认 `users/<用户ID>/photos/` 下出现对象。
4. 完全关闭手机网页后重新打开，登录同一账号，确认图片自动出现。
5. 在手机新增另一张测试图片，再回电脑刷新验证双向同步。
6. 删除测试照片，确认仅当前账号对象被删除，其他账号不受影响。
7. 最后确认 OSS ACL 仍为“私有”。

### 0.9 故障代码与含义

| 现象 | 含义 | 检查项 |
|------|------|--------|
| Edge Function `401` | 登录令牌未被函数接受 | Legacy JWT 开关应关闭；检查函数内部 `auth.getUser()` 与浏览器会话 |
| Edge Function `403` | 用户路径不属于当前账号 | 请求路径必须以 `users/<当前 user.id>/` 开头 |
| OSS PUT `403` | RAM 策略、Bucket 名或资源路径错误 | 检查策略已授权给正确 RAM 用户，资源指向香港 Bucket |
| `SignatureDoesNotMatch` | PUT 请求头与签名时不一致 | `content-type` 必须同时参与签名并由浏览器用相同值上传 |
| OSS GET `404 NoSuchKey` | 数据库有路径，但 OSS 中对象不存在 | 不能靠刷新修复；需要重新上传原图或从备份恢复 |
| 页面反复 GET 404 | 页面正在重试读取已丢失对象 | 关闭页面，先保护本地 IndexedDB/备份，再排查 |
| 退出时报 `travel_snapshots_user_id_fkey` | 浏览器仍有旧会话，但对应 Auth 用户已被删除 | 新版会清除失效会话并保留本机照片；重新注册后再迁移 |

### 0.10 省份视图卡片与动态连线（2026-08-15）

> 注：本节描述的是省份视图最初版行为；2026-08-15 下午的迭代（常亮范围收紧、防交叉连线、尺寸放大等）见 0.13 节，冲突处以 0.13 为准。

- 点击“省份视图”后，34 个省级区域全部以蓝色常亮；有已点亮城市的省份显示更强的高亮。
- 省份总览显示当前全部已点亮城市。城市卡片展示封面、城市名、推荐指数和城市简介；点击卡片仍可打开对应城市记录。
- 卡片布局会先在城市周围寻找不冲突的位置；空间不足时使用地图可用区域的网格位置兜底，卡片之间保留安全间距。
- 卡片可用区域会避开展开的左右信息面板；面板收展、窗口尺寸变化、地图拖动或缩放后都会重新计算位置。
- SVG 弯曲连线从城市坐标连接到卡片边缘，随地图 `georoam` 事件实时更新长度、端点和弯曲控制点。
- 相关代码：`renderProvinceFocusCards()`、`layoutProvinceFocusCards()`、`applyProvinceRegionStyles()`、`setView()`。

### 0.11 2026-08-13 至 2026-08-14 云同步提交

1. `2706876` — 新增账号登录与旅行资料跨设备云同步
2. `0946393` — 修复批量添加照片时云端同步遗漏
3. `0b0b63a` — 使用阿里云 OSS 自动同步照片
4. `a369567` — 兼容现有 Supabase 用户令牌
5. `1765191` — 由 OSS 地域自动生成上传地址
6. `1819cb0` — 签入 OSS 上传内容类型
7. `875dc17` — 兼容旧页面的 OSS 上传签名
8. `9ee1b61` — 防止恢复期间覆盖或删除云端照片

### 0.12 本地事故资料

`recovery/` 与 `zwxll.github.io.har` 是本次故障排查产生的本地恢复资料，不属于正式网站源代码。HAR 可能包含短期会话令牌、请求头和临时签名 URL：

- 不要提交到 GitHub。
- 不要发给无关人员。
- 确认不再需要后再由用户手工删除。

### 0.13 2026-08-15 地图 UI/UX 迭代（最新会话，均在 `F:\trae\Your-China-Travel\index.html`）

1. **省份视图常亮逻辑收紧**：`applyProvinceRegionStyles()` 由三级判断改为两级——只有**已点亮过城市的省份**才蓝色常亮，未点亮省份与城市视图一样保持深色（`#050505`）。
2. **卡片布局拉远**：径向候选半径桌面端 `[96,138,184,232,284]`、移动端 `[48,86,128,170]`；连线弯曲上限 30→40px、比例 20%→22%，视觉接近"照片墙牵线"参考图。
3. **连线防交叉**：`layoutProvinceFocusCards()` 新增 `linkSegments` 线段登记与 `segCross()`（快速排斥 + 跨立实验）检测，候选位置排序优先"不与已绘连线交叉"；网格兜底位置同样应用该优先级；所有可用位置都交叉时退化为选最短。
4. **连线与卡片无缝衔接**：连线端点沿方向延伸 8px 深入卡片矩形内部，由卡片（DOM 层在 SVG 之上）遮盖延伸段，消除边缘缝隙。
5. **点击省份显示省内全部城市名**：`updateScatterData()` 注入数据项级标签——未点亮城市灰色 `#a8bfd4` 10.5px、已点亮城市青色 `#38bdf8` 11.5px 加粗，均含 `formatter:p=>p.data.name`（缺失 formatter 会渲染成经纬度数组，已修复过一次）。
6. **已点亮城市名显示阈值**：`LABEL_ZOOM_THRESHOLD` 由 2.5 调整为 1.5（中途试过 1.0，最终取 1.5）；选中省份时省内城市标签强制显示，不受阈值限制；`computeLabelPositions()` 重建标签对象时保留数据项 `show` 状态。
7. **点击空白区域取消省份选中**：`myChart.getZr().on('click')`，zrender 点击事件无 `e.target` 即为空白（海洋/省份外），执行 `clearProvinceFocus()`；省份视图下再调用 `renderProvinceFocusCards('')` 恢复"全部已点亮城市卡片"默认态。
8. **ECharts 全局字体栈**：初始化 option 顶层 `textStyle.fontFamily` = `-apple-system,BlinkMacSystemFont,"PingFang SC","HarmonyOS Sans SC","Microsoft YaHei",sans-serif`，省份名/城市名/tooltip 统一继承。
9. **省份卡片尺寸放大 1.2 倍**：CSS 卡片 128×58 → 154×70px，封面列 52→62px，城市名 11→13px、正文 8→10px；`layoutProvinceFocusCards()` 的 `cardW/cardH` 同步为 154/70。

### 0.14 已知未解决问题：点击省份时已点亮城市名不显示（**已于 2026-08-16 前解决，见文末注**）

> **状态更新（2026-08-16）**：该问题已按下方"备选绕过方案 2"解决——当前代码中 series[2] 为独立 `type:'scatter'`、`silent:true`、`symbolSize:0.1` 的"已记录城市名"标注系列，专职承载已点亮城市名，effectScatter 上的标签已移除。`updateScatterData()` 与 `computeLabelPositions()` 均向该系列注入数据项级标签。本节保留为排查过程记录。

**现象**：点击任意省份，省份高亮、已点亮城市卡片、未点亮城市灰色名字全部正常，唯独已点亮城市（effectScatter 系列）的名字不出现。

**关键代码位置**：`updateScatterData()`（约 5209 行起）——visited 分支注入完整自包含标签配置（show/position/formatter/color/fontSize/textShadow/overflow 全套）。

**已完成排查（均通过像素级扫描验证，结论可靠）**：

- effectScatter **数据项标签机制本身正常**：在旧版应用（8765 端口副本）的真实图表实例上注入与本问题完全相同的标签配置，标签成功渲染（4 组对照实验：完整配置、切换全局字体后、`labelLayout` 组合调用、series 级 show:false + 数据项 show:true，全部渲染成功）。
- 排除 ECharts 版本差异：新旧两份代码均加载 5.4.3。
- 排除全局字体栈影响：应用 `-apple-system...` 字体栈后标签仍正常渲染。
- 排除 `labelLayout:{hideOverlap:true}`（series[0]）与 `moveOverlap:'shiftY'`（series[1]）组合的干扰。
- 排除数据形状问题：同一份 cityPoints 项（name/value/cityKey/adcode）注入即渲染。

**未完成**：在最新 index.html + 种子数据环境下完整复现点击省份流程（测试环境搭建被中断，详见 0.15）。

**下一步建议（按优先级）**：

1. 在 `F:\codex\codex-list\projects\Your-China-Travel\` 下新建 `_newtest\` 子目录并放入最新 index.html，通过 `http://localhost:8765/_newtest/` 访问（同源复用 8765 的种子 IndexedDB 数据：6 城市 11 照片，江苏有常州/苏州/镇江 3 个已点亮城市）。点击江苏省后：
   - 读 `echarts.getInstanceById(chartDom.getAttribute('_echarts_instance_')).getOption().series[1].data`，确认数据项是否带 label；
   - 若带 label 但屏幕无字 → 渲染层问题，逐项去掉配置字段定位（重点怀疑 `width:70 + overflow:'truncate'` 与 textShadow 组合）；
   - 若不带 label → 说明 `updateScatterData` 未执行或被后续 setOption 覆盖，追调用时序。
2. **备选绕过方案（机制已验证 100% 可靠）**：新增独立 `type:'scatter'`、`silent:true`、`symbolSize:0.1` 的"城市名标注"系列承载已点亮城市名，把 effectScatter 上的标签彻底移除。scatter 系列的数据项标签在全部实验中从未失效。
3. 复现环境搭建好后，同时验证 `computeLabelPositions()` 在 zoom≥1.5 重建标签时 `selectedProvinceName` 非空的分支（省份选中 + 放大叠加场景）。

### 0.15 本地测试环境状态（2026-08-15）

- **8765 端口**：旧版应用服务器（python http.server，进程约 PID 8444，无 `--directory` 参数），实际根目录 `F:\codex\codex-list\projects\Your-China-Travel`。其源内 IndexedDB 有种子数据（南昌×6、常州、苏州、镇江、常德、张家界照片）。**不要覆盖该目录的 index.html**（用户已明确拒绝过）。
- **8902 端口**：本次新建的 PowerShell HttpListener 服务器，服务 `F:\trae\Your-China-Travel`（脚本 `_http_server.ps1`，用 `C:\Users\86177\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\powershell\pwsh.exe` 启动，系统无 python/node/powershell 命令）。源无种子数据。
- 测试浏览器（TRAE 内置）当前打开 8765 与 8902 两个标签；8765 页面上有本次实验注入的临时标签配置（刷新即恢复）。
- **待清理**：`F:\trae\Project-001-Your-China-Travel\Your-China-Travel\_newtest\`（误建目录，8765 服务器根目录判断错误所致，无引用，可删）；项目根的 `_font_label_test.html` 与 `_http_server.ps1` 为实验脚本，确认无用后可删，不要提交到 GitHub。
- **浏览器工具限制**：navigate 禁止 file:// URL；evaluate 桥不 await Promise（用 document.title 中转异步结果）、返回值超过约 25KB 会被截断（大 JSON 需分块）；浏览器截图管道在高频涟漪动画下可能超时（改用 canvas getImageData 像素扫描验证渲染结果）。

### 0.16 2026-08-16 卡片位置锁定 + 深浅主题 + 配色体系（均在 `F:\trae\Your-China-Travel\index.html`）

#### A. 省份卡片位置彻底固定（仅鼠标拖动可移动）

**需求演变**：用户要求禁止卡片一切自动移动，仅保留鼠标拖动卡片时的移动；且刷新页面后位置保持。

**根因**：切换视图、点击省份、点击空白等场景会**销毁并重建卡片 DOM**，新卡片丢失位置记忆，重新走初始布局算法导致"卡片换位置"。

**实现要点**：

1. **位置持久化**：卡片初始布局完成、用户拖动结束后，把每张卡片的位置（百分比坐标）写入 localStorage；卡片重建时优先从缓存恢复位置并跳过入场动画。刷新页面后位置依然保留。
2. **手势判定（防幽灵点击）**：`myChart.getZr()` 的 `mousedown`/`mouseup` 记录位移，超过 5px 视为地图拖动手势，其后的 `click` 事件被忽略，防止拖动地图误触发省份切换/卡片重建。
3. **平移/缩放几何真值判定**（georoam 事件内）：取已点亮城市像素坐标数组，若任意两城市间距变化均小于阈值（`max(2px, 距离×2%)`）则判定为**纯平移**——锁定卡片不动，仅重绘连线端点；否则（间距变化）视为缩放，走全量布局。触控板双指横滑带轻微缩放分量时该判定依然可靠（早期"zoom 是否变化"的判定法已被淘汰）。

#### B. 地图拖动/缩放时连线长短恒定

- 平移时：每张卡片按**各自城市点的位移矢量**跟随移动（非全体均匀位移），卡片与城市相对位置不变，连线长度与弯曲恒定，仅连线端点实时重绘。
- 缩放时：应用与平移相同的"跟随城市位移"逻辑，卡片-城市相对位置、连线长短弯曲均不变。
- 跟随后的新位置防抖写回 localStorage，重建卡片/刷新页面后渲染一致。
- 手动拖动卡片或取消固定（unpin）时重置相关标志，恢复避让逻辑。

#### C. 深色/浅色主题切换（2026-08-16 新增功能）

- **入口**：header 右上角（城市照片统计旁）新增 `#themeToggleBtn`，显示"浅色/深色"文字 + 太阳/月亮图标，图标表示"点击后将切换到"的主题。
- **实现**：`body.light-theme` 类名切换 + `:root` CSS 变量覆盖（第 99-114 行）+ localStorage 键 `travelTheme` 持久化（默认深色）。
- **地图配色中枢**：`chartThemeColors()`（约 4983 行）返回当前主题的地图全套配色（区域/边界/省份名/城市名/悬浮态/选中态/tooltip 等）；`applyChartTheme()` 在切换时同步 ECharts option，并依次调用 `applyProvinceRegionStyles()` → `updateScatterData()`（数据项级标签颜色写死在数据里，必须重建刷新）→ `recomputeLabelPositions()`。
- **浅色主题深色残留修复**：补齐全屏网格视图容器背景、省份筛选胶囊（prov-pill）、视图切换轨道（view-tabs）、底部 toast 提示条、左右面板折叠箭头按钮、滚动条等组件的 `body.light-theme` 覆盖规则。

#### D. 浅色主题地图标注配色体系（经多轮用户反馈调优）

当前值（`chartThemeColors()` 浅色分支）：

| 元素 | 颜色 | 演变过程 |
|------|------|----------|
| 省份名 label | `#475569` 蓝灰 + 白描边 | #0284c7 → #4f46e5 靛蓝（用户嫌太亮）→ **#475569** |
| 悬浮省份名 emLabel | `#1e293b` 深蓝灰 | #0369a1 → #4338ca → **#1e293b** |
| 已点亮城市名 cityLabel | `#e11d48` 玫红 + 白描边 | #075985 深蓝（看不清）→ **#e11d48** |
| 未点亮城市名 normalLabel | `#4338ca` 靛蓝 + 白描边（新增主题字段） | 写死 #a8bfd4 → **跟随主题** |
| 选中省份标签 selectedLabel | `#be123c` 亮玫红 | #0369a1 → **#be123c** |

**用户审美偏好（重要）**：地图城市名要"亮"（醒目玫红/靛蓝），省份名要"沉稳"（蓝灰），header 标题要"亮天蓝"，"取消点亮"按钮要"不亮不暗"（暗玫瑰红）。调整颜色前先参考此偏好，避免反复。

**修复的 3 处写死颜色覆盖主题的 bug**（此前"改主题色不生效"的根源）：

1. `updateScatterData()` 数据项级城市名标签：写死 `#38bdf8` + 黑描边 → 改为 `chartThemeColors()` 取色。
2. `computeLabelPositions()` 重建标签数据时：写死 `#38bdf8` → 改为主题取色（该函数在主题切换后立即执行，会把主题色刷回写死值）。
3. georoam 缩放处理器中省份悬浮标签：写死 `#f8fafc` 亮白 → 改为 `chartThemeColors().emLabel`。

另外 tooltip：mouseover 处理器中 textStyle 写死 `#f8fafc`、formatter HTML 内联样式写死 `#38bdf8`/`#9aa5b1`，均改为按 `isLightTheme()` 分支取色（浅色下用 `#0284c7`/`#5b6b7c`）。

#### E. header 品牌标题配色 + 文字消失 bug 修复

- **深色主题**：主标题"记录我的中国行"渐变基色 `#3a4a5c`（暗蓝灰，深底上几乎隐形）→ **`#7dd3fc` 亮青蓝** + 纯白扫光；副标题"点亮你走过的每一座城市" `#94a3b8` → **`#93c9ef` 亮天蓝** + 亮白扫光。
- **浅色主题**：主标题 **`#0ea5e9` 亮天蓝** + `#7dd3fc` 扫光；副标题 **`#0284c7` 鲜天蓝** + `#38bdf8` 扫光。
- **关键 bug（接手必读）**：浅色主题覆盖规则曾用 `background:` **简写**，简写会隐式重置 `background-clip` 为默认值，覆盖原规则的 `background-clip:text`——结果渐变填满整个标题块而文字是 `color:transparent`，**标题渲染成实心色块、文字完全消失**（用户连续两轮反馈"字体看不清/没显示"的真正原因）。**修复**：主题覆盖必须用 `background-image:`（单一属性不牵连重置），并显式补 `-webkit-background-clip:text;background-clip:text;color:transparent`。今后任何修改 Shiny Text 渐变文字的主题覆盖都要遵守此写法。

#### F. "取消点亮"按钮配色（城市编辑弹窗，#unlightBtn）

内联样式，三轮调整：`#2a1414` 深褐（用户要求换色）→ `#dc2626` 鲜红（用户嫌太亮）→ **当前 `#9f1239` 暗玫瑰红底 + 白字 + `#be123c` 边框**。危险操作保持红色系语义。

### 0.17 2026-08-17 地图市级边界 + 点评系统 + 飞机图标（已推送）

**提交记录**：
- `9759194` feat: 新增人生足迹航线回放并统一省份字体与浅色主题配色
- `2243215` feat: 优化点评星级显示、替换飞机图标并修复轨迹方向

#### A. 地图市级边界线

- 新增第二层 geo 图层加载 DataV GeoAtlas 各省地级市 GeoJSON，`map: 'china_cities'`。
- 市级边界仅在 `CITY_BORDER_ZOOM_THRESHOLD = 7.5` 以上显示，通过动态切换 geo[1] 的 map 属性实现（`'china_cities'` / `'china'`）。
- 市级边界层设置 `silent:true`、`roam:false`，避免拦截鼠标事件；所有交互由省份层 geo[0]（`roam:true`）处理。
- 边界线样式：浅色主题 `rgba(148,163,184,.55)`，深色主题 `rgba(71,85,105,.5)`，线宽 0.15px 虚线。
- 性能优化：`progressive:30`、`progressiveThreshold:30`，避免渲染大量多边形时卡顿。

#### B. 城市名显示阈值与互斥逻辑

- 已点亮城市（玫红点）名：zoom ≥ 2 时显示。
- 未点亮城市（蓝点）名：zoom ≥ 8 时显示（全国视图下）。
- 两个标签系列互斥：series[2]（已记录城市名 scatter）标签显示时禁用 series[1]（effectScatter）的 `emphasis.label.show`，防止悬停时出现两个城市名。
- ECharts 全局 `textStyle.fontFamily` 统一为 `-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC","Hiragino Sans GB","Microsoft YaHei",sans-serif`，与 body 字体一致。

#### C. 统一 SVG 星星组件（替代字符星星）

- 之前使用 `★` 字符渲染星级，在 Windows 系统上未点亮星星显示为空心轮廓/黑色矩形框。
- 改用统一 SVG 星星组件：`renderStars(rating)` 函数生成 5 颗 SVG 星星，路径 `M12 2l2.9 6.26 6.6.57-5 4.36 1.5 6.45L12 16.9l-5.99 3.74 1.5-6.45-5-4.36 6.6-.57z`，viewBox `0 0 24 24`。
- 星星容器使用 `display:inline-flex;align-items:center`，`flex-shrink:0` 确保水平对齐。
- 星星不设边框、背景、轮廓或 box-shadow，仅通过颜色区分 active（蓝色 `#38bdf8`）和 inactive（石板灰 `#64748b`）。
- 涉及修改位置：点评卡片、发布点评弹窗、城市详情页、首页城市卡片等 8 处。

#### D. 点评系统迭代

- **移除"4/5"文字**：点评卡片中 `review-card-rating` 不再显示数字评分，仅保留星级图标。
- **紧凑星星间距**：`.reviews-avg .avg-stars` 的 `gap` 归零，`font-size` 缩小至 13px，评分数字与星星间距 `gap:4px`。
- **「查看点评」按钮位置**：从标题栏移至底部操作栏（`.modal-foot`），通过 `margin-left:auto` 右对齐，样式为胶囊形带阴影和 `backdrop-filter:blur(6px)`。
- **点评弹窗 UI**：深色科技风格，顶部显示平均评分和星级，`全部点评`/`我的点评` 双标签切换，支持上传/编辑点评（自动从城市编辑页读取已有数据）。

#### E. 人生轨迹飞机图标

- 替换为 icon-icons.com 的侧视客机 SVG（`airplane-flight #119680`），单路径 612×612 viewBox。
- **修复飞机头被裁剪**：去掉 SVG 内部 `<g transform="rotate(45 306 306)">`，原旋转导致飞机超出 viewBox 边界被裁剪。
- 飞机默认朝右，飞行方向由 CSS `transform: rotate(planeAng)` 动态控制，`planeAng` 由 `atan2(dy, dx)` 计算。
- 飞机容器 48×48px，color 使用 `--fp-plane-a`（深色主题 `#7dd3fc`，浅色主题 `#818cf8`）。

#### F. 省份标签选中态字体修复

- 选中省份时 `applyProvinceRegionStyles()` 中 label 仅保留 `color` 属性，其他字体属性（`fontWeight`、`fontFamily`）完全继承 geo 级配置，避免选中时字重突变。

#### G. 散点系列 geoIndex 修复

- ECharts 散点系列（已记录城市 effectScatter、已记录城市名 scatter）必须设置 `geoIndex:1`，绑定到 `roam:true` 的 geo 图层，确保鼠标在城市区域上时滚轮缩放和拖拽功能正常响应。

### 0.18 2026-08-18 个人资料同步 + 霓虹按钮 + 地图交互 + 我的家乡（均为 `F:\trae\Your-China-Travel\index.html`）

> 说明：本节记录 2026-08-18 当天全部迭代。其中「头像/昵称/推荐指数」已单独提交（提交号见 A 节），其余功能（霓虹按钮、悬停光晕、搜索闪烁、切账号数据合并、双击省份定位、我的家乡）随本次一并提交。

#### A. 个人资料（头像/昵称）跨设备同步与修复

- 推荐指数 5 星 bug：`setStarRating()` 原来只切换父级 `.star` 的 `active` 类，SVG 默认颜色覆盖灰色导致永远 5 星；修复为同时给 SVG 增删 `inactive` 类（active 蓝 `#38bdf8`，inactive 石板灰 `#64748b`）。
- 默认昵称统一为「旅行者」：账号弹窗与点评系统 `getPublicIdentity()` 默认值一致化，去掉「匿名」前缀。
- 头像 OSS 化：`saveCroppedAvatar()` 上传到 OSS（路径 `oss:users/<用户ID>/avatars/<cloudId>.<ext>`），新增 `resolveAvatarUrl()` 把 OSS 路径解析为可访问 data URL；`buildCloudSnapshot()` 遇到旧 base64 头像时自动上传并改写为 OSS 路径。
- 头像跨设备同步：登录时调用 `migrateProfileToAccount()` 把本地 `travelUserProfile_local` 迁移到 `travelUserProfile_<用户ID>`；云端快照新增 `userProfile` 字段随账号同步。
- 头像加载优化：新增 `avatarCache` 内存缓存，首次从 OSS 下载后缓存，后续打开账号弹窗立即显示，避免占位符闪烁。
- 邮箱不在公开点评中显示，仅头像与昵称公开；头像上传限 JPG/JPEG/PNG/WEBP 且 ≤5MB；昵称 2-20 字符，禁空格/HTML/脚本。

**提交记录**：`9ddd3a7`（5 星 bug + 昵称）、`7f0c6f3`（云端快照加 userProfile）、`beb6e51`（登录迁移本地 profile）、`e64f8f2`（头像 OSS 化）、`ae3e70e`（头像内存缓存）。

#### B. 霓虹发光按钮（Originkit 风格）

- 「人生足迹」「浅色/主题切换」「账号」三个按钮统一改为 Originkit Neon Glow Button 风格：胶囊外形（radius:100）、渐变半透明填充、三层 box-shadow 光晕叠层、text-shadow、1px 悬停上浮。
- 两套配色：深色主题青蓝霓虹 `#38bdf8`，浅色主题靛紫霓虹 `#4f46e5`。
- 旋转流光：`::before` 伪元素 conic-gradient + `mask-composite:exclude` + `neon-spin` 动画（默认 4s，悬停 1.6s）；边框始终全亮，旋转流光是沿边框转动的更亮覆盖层。
- JS 兜底：对不支持 `@property` 动画插值的浏览器，用 `requestAnimationFrame` 驱动 `--neon-deg`。
- 「人生足迹」激活态霓虹更强；「账号」按钮保留原蓝色状态点 `.account-dot` 高亮不变。

#### C. 省份字体主题切换修复

- 深色页面刷新后切浅色时，省份字体出现深色描边异常：根因是 `applyChartTheme()` 只更新了文字颜色、未同步 `textShadowColor`/`textShadowBlur`。修复为在主题切换时同步更新省份标签的 `textShadowColor`、`textShadowBlur`，并补全 emphasis 的 `textShadowBlur:8`。

#### D. 城市标记悬停光晕统一收紧

- 已点亮城市（effectScatter）：`emphasis.scale 1.3`、`itemStyle.borderWidth 1`、`shadowBlur 8`；涟漪 `rippleEffect.scale 2.0`、`period 3.5`、`itemStyle.shadowBlur 8`。
- 未点亮城市（series[0] scatter）：`emphasis.scale 1.5`、`borderWidth 1`、`shadowBlur 8`。
- `initChart` 初始 option 与 `applyChartTheme` 主题切换两处需保持一致。

#### E. 搜索高亮闪烁 6 次

- 搜索栏点击城市后，`focusSearchResult()` 对该散点交替执行 `highlight`/`downplay`，`maxBlink=12`（亮→暗 12 次状态切换 = 6 个完整闪烁周期），间隔 350ms；结束后 `downplay` 恢复。

#### F. 登录其他账号时本地数据不丢（合并同步）

- 之前切账号时 `prepareUserData()` 检测「登录账号 ≠ 本地 owner」会调用 `clearLocalTravelData()` 清空本地；退出且已同步（dirty='0'）时也清空本地，导致本地内容丢失。
- 修复：删除切账号清空逻辑；登录其他账号时若目标账号无云端数据则直接同步本地到云端，若有云端数据则用 `mergeCloudSnapshotIntoLocal()` 把云端数据合并进本地（本地优先，按 cloudId/storagePath/cityKey/review.id 去重）后再同步；退出时保留本地数据与 owner 标记，便于后续登录其他账号再次迁移。

#### G. 双击省份定位放大

- 在 `dblclick` 事件新增 `geo` 分支：`params.componentType==='geo' && params.name` 时定位到该省份并放大。
- 新增辅助函数 `getChinaFeatures()` / `geomBBox()` / `chinaBBox()` / `focusProvinceOnMap()`：递归收集省份 geometry 的 [lng,lat] 点求包围盒，中心取包围盒中点，缩放级别按「省份对角线 / 全国对角线」比例换算（clamp 2~8，大省倍率小、小省倍率大），且绝不缩小（`max(target, currentZoom)`）。
- 省份单击改为延迟 250ms（`pendingProvinceClickTimer`）执行高亮，双击时取消该挂起任务避免冲突；双击已高亮省份时跳过 `selectProvinceFocus` 的 toggle 清空逻辑。
- 缩放后触发 `geoRoam` action（name `china_cities`）同步标签显隐、散点大小与省份字体。

#### H. 「我的家乡」独立入口

- 顶部导航在「人生足迹」右侧新增「我的家乡」按钮（同为霓虹发光样式、房子图标）。
- 点击后打开与「人生足迹」完全相同的家乡选择弹窗（`#fpHomeOverlay`），但进入 `fpPickerMode==='hometown'` 模式。
- `hometown` 模式下选中城市：保存家乡（`setFootprintHome`）、关闭弹窗、直接 `openCityModal(c)` 进入城市编辑页（功能与「点亮城市」完全一致），**不启动**足迹回放。
- 编辑弹窗新增「🏠 这是我的家乡」强调横幅 `#hometownBanner`：`openCityModal()` 内比对 `getFootprintHome().cityKey === city.cityKey` 控制显隐；深浅主题均有配色（`.hometown-banner`）。
- 家乡与人生足迹共用存储键 `footprintHomeCity`，设好家乡后人生足迹仍以家乡为航线起点，逻辑不受影响。

---

## 一、项目概述

**项目名称**：记录我的中国行（Chinese Footprint）

**项目定位**：前端仍为单文件 HTML 原生应用（内联 CSS/JS、无构建步骤），后端使用 Supabase 托管账号、数据库与 Edge Function，照片使用私有阿里云 OSS。

**GitHub 仓库**：`https://github.com/zwxll/Your-China-Travel.git`

**部署方式**：GitHub Pages（也可用 Vercel / Nginx / Docker）

**在线访问**：通过 GitHub Pages 自动构建，推送 main 分支后 1-2 分钟生效

---

## 二、文件结构及各文件作用

```
Your-China-Travel/
├── index.html              # 主页面（单文件，含全部内联 CSS/JS/动画，约 7400 行）
├── earth-startup.html      # 地球启动页备份（独立文件，已合并进 index.html）
├── china-geo.js            # 中国边界 GeoJSON（暴露全局 window.CHINA_GEO）
├── china-geo.json          # 中国边界 GeoJSON 原始 JSON
├── china-provinces-geo.js  # 34 省地级市边界数据（暴露全局变量）
├── README.md               # 部署文档（Vercel/Nginx/Docker 方案）
├── vercel.json             # Vercel 部署配置
├── nginx.conf              # Nginx 服务器配置
├── Dockerfile              # Docker 容器化部署
├── docker-compose.yml      # Docker 一键编排
└── .gitignore
```

---

## 三、index.html 代码各部分详解

index.html 是核心文件，约 7400 行，分为以下几大区块：

### 3.1 `<head>` 区块（第 3-2339 行）

**全局 CSS 变量定义**（`:root`，第 17-40 行）：
- 纯黑科技基底配色：`--bg1:#020202`、`--card:#080808`
- 青蓝主色：`--accent:#38bdf8`、`--accent-d:#0ea5e9`、`--accent-l:#7dd3fc`
- 深海蓝黑：`--sea1:#0a1828`、`--sea2:#08131f`
- 阴影/圆角/间距等设计 token

**主界面 CSS**（第 41-2338 行）：
- header 顶栏（毛玻璃背景 `backdrop-filter`）
- brand 品牌区（Shiny Text 闪光标题动画）
- stats 统计区（城市数/照片数）
- ECharts 地图容器
- 城市卡片网格
- 城市编辑弹窗（modal-overlay）
- 照片网格、标签管理、景点管理
- 时间线弹窗、照片预览 lightbox
- toast 提示
- ferrofluid 铁磁流体背景
- Gooey 黏稠 SVG Filter

**地球启动层 CSS**（`<head>` 末尾，约第 2340 行前）：
```css
#launch-screen { position:fixed; inset:0; z-index:50; background:#000; overflow:hidden; }
#prism-bg      { position:absolute; inset:0; z-index:2; pointer-events:none; }  /* 粒子背景 */
#globeViz      { position:absolute; inset:0; z-index:1; pointer-events:auto; }   /* 3D地球 */
.launch-overlay{ position:absolute; inset:0; z-index:10; pointer-events:none; }  /* 标题按钮 */
```
- `.launch-title-wrap` / `.launch-title` / `.launch-title-glow`：双层标题（发光层 + 扫光层），`background-clip:text` 实现 shiny 扫光
- `.launch-subtitle`：副标题
- `.launch-btn`：镜面玻璃按钮（深色玻璃底 + 暖金白文字 + 闪光图标 + 箭头 + 镜面扫光条 `::after`）
- `@keyframes shinySweep`：标题扫光动画
- `@keyframes btnSweep`：按钮镜面扫光动画

### 3.2 `<body>` HTML 结构（第 2340 行起）

```html
<!-- 底层铁磁流体背景 -->
<canvas id="ferrofluidBg" style="z-index:-1"></canvas>

<!-- Gooey SVG Filter -->
<svg>...</svg>

<!-- 地球启动层（z-index:50，初始全屏覆盖主内容）-->
<div id="launch-screen">
  <div id="prism-bg"></div>              <!-- 粒子星空背景 -->
  <div id="globeViz"></div>              <!-- globe.gl 3D地球 -->
  <div class="launch-overlay">           <!-- 标题+按钮穿透层 -->
    <div class="launch-title-wrap">
      <h1 class="launch-title-glow">Chinese footprint</h1>
      <h1 class="launch-title">Chinese footprint</h1>
    </div>
    <p class="launch-subtitle">点亮你走过的每一座城市</p>
    <button class="launch-btn" id="startBtn">✦ 开始记录 →</button>
  </div>
</div>

<!-- 页面过渡遮罩（GSAP 控制黑屏淡入淡出）-->
<div id="page-transition" style="z-index:100;opacity:0;"></div>

<!-- 主界面 -->
<header>品牌 + 统计</header>
<main>
  <aside>左侧面板：城市/省份视图 + 旅行轨迹</aside>
  <section>右侧面板：搜索 + 城市列表</section>
  <div id="chart">ECharts 地图</div>
</main>

<!-- 弹窗 -->
<div id="modalOverlay">城市编辑弹窗</div>
<div id="lightbox">照片预览</div>
<div id="timelineModalOverlay">时间线弹窗</div>
<div id="attDetailOverlay">景点详情</div>
<div id="toast">提示</div>
```

### 3.3 `<script>` 区块

**外部脚本**（`</body>` 前）：
```html
<script src="china-geo.js"></script>           <!-- 中国边界，暴露 window.CHINA_GEO -->
<script src="china-provinces-geo.js"></script> <!-- 34省地级市边界 -->
<script src="https://cdn.jsdelivr.net/npm/globe.gl"></script>      <!-- 3D地球 -->
<script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script> <!-- GSAP动画 -->
```

**GSAP 入场动画**（独立 script 块，GSAP 加载后立即执行）：
```javascript
gsap.fromTo('.launch-title-wrap', {y:40, autoAlpha:0}, {y:0, autoAlpha:1, duration:1.2, ease:'power3.out', delay:0.3});
gsap.fromTo('.launch-subtitle',   {y:30, autoAlpha:0}, {y:0, autoAlpha:1, duration:1.0, ease:'power3.out', delay:0.7});
gsap.fromTo('.launch-btn',        {y:30, autoAlpha:0}, {y:0, autoAlpha:1, duration:1.0, ease:'back.out(1.4)', delay:1.0});
```

**粒子星空背景**（`<script type="module">`，动态 import ogl）：
- 从 `https://esm.sh/ogl@1.0.11` 动态加载 ogl
- 500 个粒子，球体分布，蓝白冷色调（`#ffffff` `#a5d8ff` `#74c0fc`）
- vertex/fragment shader 实现圆形透明粒子 + 正弦扰动
- 鼠标视差：粒子整体平移跟随鼠标（lerp 缓动）
- 渲染循环：requestAnimationFrame 持续渲染

**地球初始化 + GSAP 点击时间线**（普通 script）：
```javascript
function initGlobe(){
  // 等待 globe.gl 加载
  // 初始化地球：纹理 + 凹凸贴图 + 自转 + altitude:1.5
  // GSAP 按钮 hover 动画（paused tween，mouseenter play / mouseleave reverse）
  // GSAP 点击时间线（gsap.timeline）：
  //   步骤1: 按钮按压 scale 0.94→1.0 反馈
  //   步骤2: camProxy tween 驱动镜头缓动拉近中国（lat:36 lng:105 altitude:1.3，1.6s）
  //   步骤3: 停留欣赏中国版图（0.8s）
  //   步骤4: UI 元素 autoAlpha 淡出（0.6s）
  //   步骤5: 黑屏遮罩淡入（0.5s）
  //   步骤6: gsap.set('#launch-screen', {display:'none'}) 隐藏地球层
  //   步骤7: 黑屏遮罩淡出露出主地图（0.6s）
  //   步骤8: call(() => window.dispatchEvent(new Event('resize'))) 触发 ECharts resize
  // 加载 three.js UMD → 云层 + 光照
}
```

**主业务逻辑 IIFE**（约第 3400-7400 行）：
- `PerfScheduler`：全局交互状态调度器（交互期间降帧）
- ECharts 地图初始化（4 重 CDN 回退）
- 城市数据管理（IndexedDB 存储照片 Base64）
- 城市点亮 / 编辑 / 取消点亮
- 照片上传 / 拖拽 / 预览
- 标签管理 / 景点管理
- 旅行轨迹时间线（同年同月按日期排序）
- 省份筛选 / 城市搜索
- ferrofluid 铁磁流体背景动画
- 流星特效
- Variable Proximity 标题动画

---

## 四、外部依赖（CDN 运行时加载）

| 依赖 | 用途 | CDN 来源 |
|------|------|----------|
| Inter 可变字体 | Variable Proximity 标题动画 | fonts.googleapis.com |
| ECharts 5.4.3 | 中国地图渲染/缩放/拖拽 | jsdelivr / bootcdn / unpkg（4 重回退） |
| globe.gl | 3D 云层地球 | cdn.jsdelivr.net/npm/globe.gl |
| GSAP 3.12.5 | 全页面动画引擎（timeline/缓动/GPU加速） | cdn.jsdelivr.net/npm/gsap |
| ogl 1.0.11 | 粒子星空背景 WebGL | esm.sh/ogl（动态 import） |
| three.js 0.149.0 | 云层 Mesh / 光照（UMD） | cdn.jsdelivr.net/npm/three |
| 中国 GeoJSON | 省份/地级市边界 | 本地 china-geo.js / china-provinces-geo.js |

---

## 五、完整修改历史（按时间顺序，共 34 次提交）

以下是从最早到最新的全部提交记录，完整记录了项目演进过程：

### 阶段一：项目初始上传
1. `7bcb27a` - Add files via upload（初始项目上传）
2. `5b58b1b` - Add files via upload
3. `73bd41f` - Add files via upload

### 阶段二：性能与体验优化
4. `340afbe` - perf: 静态资源(js/json/css/图片)启用7天immutable缓存
5. `8dd72ef` - fix: 34省地级市边界本地化，修复城市数从371降级为35的问题
6. `d03b3eb` - feat: 景点照片上传支持拖拽，照片同步存入景点相册和城市主相册
7. `76db29d` - feat: 城市编辑弹窗尺寸放大+布局美化+滚动隔离
8. `1ffd263` - feat: 照片网格区新增"照片管理"标题，与标签管理/景点管理样式统一
9. `73adccd` - style: 统一照片管理/标签管理/景点管理三标题字号、字重、颜色、对齐
10. `d4dfe6b` - fix: 城市封面预览拉伸变形——固定16:7宽高比容器+object-fit:cover
11. `9abb977` - style: 缩小城市编辑弹窗整体尺寸，内部排版紧凑化
12. `804aa2c` - style: 缩小城市封面预览尺寸，限宽280px不再撑满弹窗
13. `81cde10` - style: 城市封面高度固定250px+水平居中(左右空余相同)
14. `44503f6` - perf: 全局性能优化——交互期间降帧+合成层隔离+rect缓存
15. `1b5cd58` - feat: 城市编辑弹窗新增「取消点亮」按钮，一键清空当前城市所有内容
16. `a28710c` - feat: 旅行轨迹时间线排序优化——同年同月多城市按游玩日期细分排序
17. `109e393` - fix: 标签管理输入框白字白底看不见——加深色背景

### 阶段三：3D 地球启动页开发（独立文件 earth-startup.html）
18. `20115f1` - feat: 新增globe.gl云层3D地球启动页(earth-startup.html)
19. `e95315d` - fix: 修复earth-startup.html双击file://打开看不到3D地球的问题
20. `fcec565` - style: 调整启动页地球尺寸——altitude 2.5→3.6 缩小至约视口45%
21. `55e3a6d` - style: 启动页地球放大——altitude 3.6→1.3 接近全屏铺满
22. `175e86b` - style: 启动页标题居中+按钮参考照片风格
23. `faacc03` - feat: 启动页交互完善——全球视角+镜头过渡到中国+去除高光

### 阶段四：动态背景迭代（earth-startup.html）
24. `eff48d7` - feat: 启动页新增reactbits Prism棱镜动态背景
25. `f938269` - fix: 棱镜背景改CSS实现——解决file://不显示+地球拖拽卡顿
26. `a4aaa02` - fix: 修复棱镜光流背景不可见——调整图层位置到地球上方
27. `1fcf7d5` - feat: 棱镜背景改用Canvas2D——流动光线+粒子+极光波动
28. `f2256f2` - feat: 背景替换为reactbits Particles粒子星空
29. `97c9770` - feat: 粒子密度和亮度大幅提升+鼠标交互
30. `2f95943` - style: 调整粒子大小和配色比例
31. `e295e67` - style: 地球尺寸放大——altitude 2.5→1.5

### 阶段五：HyperFrames/GSAP 动画重构
32. `2d1273a` - feat: HyperFrames/GSAP重构全部动画——timeline串联+GPU加速
    - 引入 GSAP 3.12.5，用 gsap.timeline 串联所有动画
    - 入场动画（gsap.fromTo 标题/副标题/按钮）
    - 按钮 hover（GSAP paused tween）
    - 点击时间线（按压→镜头→停留→UI淡出→黑屏→跳转）
    - 新增 #page-transition 过渡遮罩

### 阶段六：合并进主项目 + 最终优化
33. `cb1344e` - feat: 地球启动页合并进主项目index.html——单文件整合+GSAP动画
    - 将 earth-startup.html 完整合并进 index.html
    - #launch-screen 全屏容器 z-index:50 覆盖主内容
    - 点击跳转改为 gsap.set display:none 隐藏地球层
    - 新增中国国界呼吸高光特效（后移除）
34. `e5ecaa2` - perf: 优化粒子互动卡顿——降粒子数+简化鼠标交互
    - particleCount 800→500
    - 移除鼠标旋转偏移，只保留平移视差

---

## 六、关键技术决策与实现细节

### 6.1 3D 地球（globe.gl）
- **为何用 globe.gl**：封装了 three.js，API 简洁，支持 UMD 加载（file:// 可用）
- **云层实现**：globe.gl 不暴露全局 THREE，需额外动态加载 three@0.149.0 UMD 创建云层 Mesh
- **镜头控制**：globe.gl 的 `pointOfView()` 不支持 GSAP 直接 tween，用 camProxy 代理对象 + onUpdate 回调驱动
- **初始视角**：`altitude:1.5` 让地球占满大部分屏幕（值越小地球越大）

### 6.2 粒子星空背景（ogl）
- **为何用 ogl 而非 Canvas 2D**：reactbits Particles 组件基于 ogl 实现 3D 粒子（Points + 透视相机 + 球体分布），Canvas 2D 难以还原
- **ESM 动态 import**：`import('https://esm.sh/ogl@1.0.11')`，file:// 下粒子不显示但地球正常（优雅降级）
- **性能优化**：粒子数从 800 降到 500，移除鼠标旋转偏移，避免和 globe.gl 两个 WebGL context 争抢 GPU

### 6.3 GSAP 动画引擎
- **为何用 GSAP**：GPU 加速 transform/opacity，规避 setTimeout 卡顿，timeline 串联多步骤动画
- **三层动画解耦**：粒子(ogl) / 地球(globe.gl) / UI文字按钮(GSAP) 互不抢占渲染资源
- **入场动画用 fromTo 而非 from**：明确起止状态，避免 CSS opacity:0 冲突
- **autoAlpha 替代 opacity**：同时控制 opacity + visibility，防闪烁

### 6.4 页面切换（非跳转）
- **原方案**：`window.location.href = './index.html'` 跳转到主页面
- **合并后方案**：GSAP 隐藏 `#launch-screen`（display:none）+ 触发 `window.dispatchEvent(new Event('resize'))` 让 ECharts 重新计算尺寸
- **过渡流程**：黑屏淡入 → 隐藏地球层 → 黑屏淡出 → resize

### 6.5 数据存储
- 未登录时照片以 Base64/Data URL 存入 IndexedDB，仅当前设备可见
- 登录后 Supabase 保存旅行快照，私有阿里云 OSS 保存实际照片，IndexedDB 作为本机缓存
- 同一账号可跨设备自动恢复；不同账号通过 RLS、用户目录与 Edge Function 路径校验相互隔离

---

## 七、推送 GitHub 全过程

### 7.1 环境配置
```
Git 路径：C:\Program Files\Git\cmd\git.exe
GitHub CLI 路径：C:\Program Files\GitHub CLI
项目目录：F:\codex\codex-list\projects\Your-China-Travel
远程仓库：https://github.com/zwxll/Your-China-Travel.git
分支：main
```

### 7.2 标准推送命令（PowerShell）
```powershell
$env:PATH='C:\Program Files\Git\cmd;C:\Program Files\Git\bin;C:\Program Files\Git\mingw64\bin;C:\Program Files\GitHub CLI;'+$env:PATH
$git='C:\Program Files\Git\cmd\git.exe'
Set-Location 'F:\codex\codex-list\projects\Your-China-Travel'
& $git add -A
& $git commit -m "feat: 提交信息"
& $git push
```

### 7.3 注意事项
- PowerShell 中用 `;` 分隔命令（非 `&&`）
- 文件路径用单引号（PowerShell 单引号是字面量）
- GitHub 网络偶有超时，push 失败时重试即可
- commit message 用中文，遵循 conventional commits 格式（feat/fix/style/perf）
- 推送后 GitHub Pages 自动构建，1-2 分钟后生效，需强制刷新（Ctrl+Shift+R）

### 7.4 部署
- GitHub Pages：仓库 Settings → Pages → main 分支自动部署
- 也可用 Vercel（vercel.json 已配置）/ Nginx（nginx.conf）/ Docker（Dockerfile + docker-compose.yml）

---

## 八、当前最终状态

### 8.1 最终文件结构
- `index.html`：单文件主页面，包含地球启动层 + 主地图 + 全部业务逻辑
- `earth-startup.html`：独立地球启动页备份（保留，未删除）
- 其他配置文件不变

### 8.2 最终动画效果
1. 页面加载 → 地球启动层全屏覆盖主内容
2. GSAP 入场动画：标题 → 副标题 → 按钮依次浮入
3. 地球自动缓慢自转 + 粒子星空背景流动
4. 鼠标可穿透粒子层拖拽地球 / 滚轮缩放
5. 粒子随鼠标平移产生视差效果
6. 点击「开始记录」→ GSAP timeline：
   - 按钮按压反馈 → 镜头缓动聚焦中国 → 停留 → UI淡出 → 黑屏过渡 → 隐藏地球层 → 黑屏淡出 → 主地图显示
7. 主地图：ECharts 中国地图 + 城市点亮 + 照片上传 + 轨迹时间线等全部功能

### 8.3 关键参数
| 参数 | 值 | 位置 |
|------|-----|------|
| 粒子数 particleCount | 500 | index.html 粒子脚本 |
| 粒子配色 | #ffffff #a5d8ff #74c0fc | 同上 |
| 粒子大小 particleBaseSize | 100 | 同上 |
| 地球初始 altitude | 1.5 | initGlobe |
| 镜头聚焦 altitude | 1.3 | 点击时间线步骤2 |
| GSAP CDN | 3.12.5 | head script |
| globe.gl | latest | head script |

---

## 九、给接手 Agent 的建议

1. **修改 index.html 时注意行数会变化**：文件约 7400 行，修改后行号偏移，用 Grep 定位而非固定行号
2. **地球启动层脚本在主业务 IIFE 之前**：注意脚本加载顺序，globe.gl 和 GSAP 需在 initGlobe 前加载
3. **粒子脚本用 type="module"**：动态 import ogl，与普通 script 隔离
4. **测试时用 http-server**：`npx http-server -p 8782 -c-1 --cors`，file:// 下粒子不显示
5. **浏览器测试用 integrated_browser MCP**：navigate → wait_for → evaluate → take_screenshot
6. **推送前先停 http-server**：避免端口占用
7. **GitHub 偶有网络超时**：push 失败重试即可，commit 不会丢失
8. **earth-startup.html 保留作备份**：不要删除，是独立地球页的完整实现

---

*初始文档生成时间：2026-07-20*
*当前基线更新时间：2026-08-17*
*最新提交：2243215 feat: 优化点评星级显示、替换飞机图标并修复轨迹方向*
*最近未提交迭代：0.17 节全部内容（2026-08-17，已推送）*
