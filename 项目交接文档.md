# 「记录我的中国行」项目交接文档

> 本文档完整记录项目代码结构、各部分功能、所有修改过程及 GitHub 推送全流程，供交接给其他 Agent 使用。

> **当前基线提示（2026-08-14）**：项目已经接入 Supabase 账号与阿里云 OSS 跨设备照片同步。下方“〇、当前生产环境交接”是最新权威说明；后面的旧章节保留为历史记录，如有冲突，以“〇”章节为准。

---

## 〇、当前生产环境交接（2026-08-14，权威基线）

### 0.1 当前完成状态

- 正式网站：`https://zwxll.github.io/Your-China-Travel/`
- GitHub 仓库：`https://github.com/zwxll/Your-China-Travel.git`
- 当前工作目录：`F:\codex\codex-list\projects\Your-China-Travel`
- 账号系统：Supabase Auth，支持用户自行注册、登录、退出及跨设备登录。
- 旅行数据：Supabase PostgreSQL 表 `public.travel_snapshots`，每个用户一行快照。
- 照片文件：阿里云 OSS 私有 Bucket `your-china-travel-8877-hk`，地域为中国香港 `oss-cn-hongkong`。
- 本机缓存：IndexedDB 数据库 `TravelPhotosDB`，对象仓库为 `photos` 与 `cityMeta`。
- OSS 签名服务：Supabase Edge Function `oss-media`。
- 云同步安全基线提交：`9ee1b61 fix: 防止恢复期间覆盖或删除云端照片`。

### 0.2 当前架构与数据流

```text
浏览器
  ├─ Supabase Auth：注册、登录、会话
  ├─ IndexedDB：当前设备缓存照片与城市资料
  ├─ travel_snapshots：保存照片路径、城市、顺序、元数据
  └─ 调用 oss-media Edge Function
       ├─ 验证 Supabase 登录令牌
       ├─ 验证路径必须属于 users/<当前用户ID>/
       └─ 返回 5 分钟有效的 OSS PUT / GET 临时签名地址
            └─ 私有阿里云 OSS：保存实际图片文件
```

未登录时，数据只保存在当前浏览器 IndexedDB。登录后，页面自动把旅行清单同步到 Supabase，把实际照片上传到 OSS；换设备登录同一账号后，页面自动恢复云端内容。IndexedDB 登录后仍保留为本机缓存，不是唯一存储。

### 0.3 用户权限语义

- 所有人都可以在网站注册账号。
- 已登录用户可以新增、查看、删除**自己账号**的照片。
- 用户 A 不能读取或删除用户 B 的照片。
- OSS 必须保持“私有”，不要设置成“公共读写”。公共读写代表绕过网站登录即可直接操作 OSS，不等于“允许所有登录用户使用”。
- 照片路径格式：`users/<Supabase user id>/photos/<uuid>.<ext>`。
- `oss-media` 的 `ownsPath()` 会在签名和删除前校验当前用户 ID，RAM 策略只负责限制专用 RAM 用户可以操作的 Bucket 范围。

### 0.4 云端配置清单

| 项目 | 当前值 / 要求 |
|------|---------------|
| Supabase Project Ref | `zpqbbawremufcxxszgde` |
| Supabase 数据表 | `public.travel_snapshots`，已启用 RLS |
| Edge Function | `oss-media` |
| Verify JWT with legacy secret | **关闭**；函数内部仍调用 `auth.getUser(token)` 验证登录用户 |
| OSS Bucket | `your-china-travel-8877-hk` |
| OSS Region | `oss-cn-hongkong` / `cn-hongkong`（SDK 使用配置中的 Region） |
| OSS ACL | **私有** |
| 正式 CORS Origin | `https://zwxll.github.io` |
| 本地 CORS Origin | `http://127.0.0.1:8765` |
| 临时签名有效期 | 300 秒 |

Supabase Edge Function Secrets 必须存在：

```text
ALIYUN_OSS_ACCESS_KEY_ID
ALIYUN_OSS_ACCESS_KEY_SECRET
ALIYUN_OSS_BUCKET
ALIYUN_OSS_REGION
```

AccessKey Secret、Supabase Secret key、service_role key 禁止写入 `index.html`、`supabase-config.js`、Markdown、HAR 或 Git 仓库。`supabase-config.js` 只能放浏览器可公开的 Project URL 与 Publishable key。

### 0.5 OSS CORS 与 RAM 权限

OSS CORS 至少包含：

- 来源：`https://zwxll.github.io`、`http://127.0.0.1:8765`
- Methods：`GET`、`PUT`、`DELETE`、`HEAD`
- Allowed Headers：`*`
- Expose Headers：`ETag`、`x-oss-request-id`
- Max Age：600 秒

专用 RAM 用户只授权当前 Bucket 的对象读取、上传和删除，不给控制台管理权限。当前自定义策略名为 `YourChinaTravelOSSAccess`。策略资源必须指向新的香港 Bucket，例如：

```text
acs:oss:*:*:your-china-travel-8877-hk/users/*
```

### 0.6 关键文件

```text
Your-China-Travel/
├── index.html                         # 主页面、IndexedDB、账号与同步逻辑
├── supabase-config.js                 # 浏览器端公开 Supabase 配置
├── supabase-setup.sql                 # travel_snapshots、RLS、旧 Supabase Storage 兼容配置
├── supabase/functions/oss-media/
│   └── index.ts                       # 登录验证、用户路径校验、OSS V4 签名与删除
├── README.md                          # 面向使用者的部署与配置说明
├── PROJECT_MEMORY.md                  # 面向后续 Agent 的快速记忆
└── 项目交接文档.md                    # 完整交接文档
```

`supabase-setup.sql` 中的 `travel-media` Supabase Storage Bucket 是旧方案兼容项；当前新增照片的主存储是阿里云 OSS。

### 0.7 同步安全逻辑

- 批量添加照片时，会等待所有 IndexedDB 写入完成后再生成云端快照。
- 云端恢复期间设置 `cloudSuppressDirty`，禁止把半恢复或空状态反向覆盖云端。
- 恢复前先下载并准备全部照片与元数据，全部成功后才清空本地缓存并写入。
- 恢复开始时会取消已排队的自动同步定时器。
- 同一账号刷新时，如果本机记录的云端更新时间与 `travel_snapshots.updated_at` 一致，并且 IndexedDB 含有快照需要的全部图片路径，则直接使用本机缓存，不调用 OSS 下载。
- 其他设备产生新快照时，恢复流程按 `storagePath` 复用 IndexedDB 已有图片，只向 OSS 下载本机缺少或路径发生变化的图片。
- 当前版本不再自动删除“未出现在快照中的 OSS 文件”，避免竞态条件误删照片；删除照片只走用户明确触发的删除流程。
- 首次登录空账号且本机有资料时，会迁移本机资料；云端与本机同时有资料时，应由用户选择保留哪一份。
- 不要连续点击“账号同步”、退出、重新登录；应等待顶部状态显示“已同步”。

### 0.8 生产验证流程

每次修改同步代码或 Edge Function 后按以下顺序验证：

1. 电脑登录测试账号，新增一张容易识别的测试图片。
2. 等顶部显示“已同步”，不要手工重复触发同步。
3. 在 OSS 控制台确认 `users/<用户ID>/photos/` 下出现对象。
4. 完全关闭手机网页后重新打开，登录同一账号，确认图片自动出现。
5. 在手机新增另一张测试图片，再回电脑刷新验证双向同步。
6. 删除测试照片，确认仅当前账号对象被删除，其他账号不受影响。
7. 最后确认 OSS ACL 仍为“私有”。

### 0.9 故障代码与含义

| 现象 | 含义 | 检查项 |
|------|------|--------|
| Edge Function `401` | 登录令牌未被函数接受 | Legacy JWT 开关应关闭；检查函数内部 `auth.getUser()` 与浏览器会话 |
| Edge Function `403` | 用户路径不属于当前账号 | 请求路径必须以 `users/<当前 user.id>/` 开头 |
| OSS PUT `403` | RAM 策略、Bucket 名或资源路径错误 | 检查策略已授权给正确 RAM 用户，资源指向香港 Bucket |
| `SignatureDoesNotMatch` | PUT 请求头与签名时不一致 | `content-type` 必须同时参与签名并由浏览器用相同值上传 |
| OSS GET `404 NoSuchKey` | 数据库有路径，但 OSS 中对象不存在 | 不能靠刷新修复；需要重新上传原图或从备份恢复 |
| 页面反复 GET 404 | 页面正在重试读取已丢失对象 | 关闭页面，先保护本地 IndexedDB/备份，再排查 |

### 0.10 2026-08-13 至 2026-08-14 云同步提交

1. `2706876` — 新增账号登录与旅行资料跨设备云同步
2. `0946393` — 修复批量添加照片时云端同步遗漏
3. `0b0b63a` — 使用阿里云 OSS 自动同步照片
4. `a369567` — 兼容现有 Supabase 用户令牌
5. `1765191` — 由 OSS 地域自动生成上传地址
6. `1819cb0` — 签入 OSS 上传内容类型
7. `875dc17` — 兼容旧页面的 OSS 上传签名
8. `9ee1b61` — 防止恢复期间覆盖或删除云端照片

### 0.11 本地事故资料

`recovery/` 与 `zwxll.github.io.har` 是本次故障排查产生的本地恢复资料，不属于正式网站源代码。HAR 可能包含短期会话令牌、请求头和临时签名 URL：

- 不要提交到 GitHub。
- 不要发给无关人员。
- 确认不再需要后再由用户手工删除。

---

## 一、项目概述

**项目名称**：记录我的中国行（Chinese Footprint）

**项目定位**：前端仍为单文件 HTML 原生应用（内联 CSS/JS、无构建步骤），后端使用 Supabase 托管账号、数据库与 Edge Function，照片使用私有阿里云 OSS。

**GitHub 仓库**：`https://github.com/zwxll/Your-China-Travel.git`

**部署方式**：GitHub Pages（也可用 Vercel / Nginx / Docker）

**在线访问**：通过 GitHub Pages 自动构建，推送 main 分支后 1-2 分钟生效

---

## 二、文件结构及各文件作用

```
Your-China-Travel/
├── index.html              # 主页面（单文件，含全部内联 CSS/JS/动画，约 7400 行）
├── earth-startup.html      # 地球启动页备份（独立文件，已合并进 index.html）
├── china-geo.js            # 中国边界 GeoJSON（暴露全局 window.CHINA_GEO）
├── china-geo.json          # 中国边界 GeoJSON 原始 JSON
├── china-provinces-geo.js  # 34 省地级市边界数据（暴露全局变量）
├── README.md               # 部署文档（Vercel/Nginx/Docker 方案）
├── vercel.json             # Vercel 部署配置
├── nginx.conf              # Nginx 服务器配置
├── Dockerfile              # Docker 容器化部署
├── docker-compose.yml      # Docker 一键编排
└── .gitignore
```

---

## 三、index.html 代码各部分详解

index.html 是核心文件，约 7400 行，分为以下几大区块：

### 3.1 `<head>` 区块（第 3-2339 行）

**全局 CSS 变量定义**（`:root`，第 17-40 行）：
- 纯黑科技基底配色：`--bg1:#020202`、`--card:#080808`
- 青蓝主色：`--accent:#38bdf8`、`--accent-d:#0ea5e9`、`--accent-l:#7dd3fc`
- 深海蓝黑：`--sea1:#0a1828`、`--sea2:#08131f`
- 阴影/圆角/间距等设计 token

**主界面 CSS**（第 41-2338 行）：
- header 顶栏（毛玻璃背景 `backdrop-filter`）
- brand 品牌区（Shiny Text 闪光标题动画）
- stats 统计区（城市数/照片数）
- ECharts 地图容器
- 城市卡片网格
- 城市编辑弹窗（modal-overlay）
- 照片网格、标签管理、景点管理
- 时间线弹窗、照片预览 lightbox
- toast 提示
- ferrofluid 铁磁流体背景
- Gooey 黏稠 SVG Filter

**地球启动层 CSS**（`<head>` 末尾，约第 2340 行前）：
```css
#launch-screen { position:fixed; inset:0; z-index:50; background:#000; overflow:hidden; }
#prism-bg      { position:absolute; inset:0; z-index:2; pointer-events:none; }  /* 粒子背景 */
#globeViz      { position:absolute; inset:0; z-index:1; pointer-events:auto; }   /* 3D地球 */
.launch-overlay{ position:absolute; inset:0; z-index:10; pointer-events:none; }  /* 标题按钮 */
```
- `.launch-title-wrap` / `.launch-title` / `.launch-title-glow`：双层标题（发光层 + 扫光层），`background-clip:text` 实现 shiny 扫光
- `.launch-subtitle`：副标题
- `.launch-btn`：镜面玻璃按钮（深色玻璃底 + 暖金白文字 + 闪光图标 + 箭头 + 镜面扫光条 `::after`）
- `@keyframes shinySweep`：标题扫光动画
- `@keyframes btnSweep`：按钮镜面扫光动画

### 3.2 `<body>` HTML 结构（第 2340 行起）

```html
<!-- 底层铁磁流体背景 -->
<canvas id="ferrofluidBg" style="z-index:-1"></canvas>

<!-- Gooey SVG Filter -->
<svg>...</svg>

<!-- 地球启动层（z-index:50，初始全屏覆盖主内容）-->
<div id="launch-screen">
  <div id="prism-bg"></div>              <!-- 粒子星空背景 -->
  <div id="globeViz"></div>              <!-- globe.gl 3D地球 -->
  <div class="launch-overlay">           <!-- 标题+按钮穿透层 -->
    <div class="launch-title-wrap">
      <h1 class="launch-title-glow">Chinese footprint</h1>
      <h1 class="launch-title">Chinese footprint</h1>
    </div>
    <p class="launch-subtitle">点亮你走过的每一座城市</p>
    <button class="launch-btn" id="startBtn">✦ 开始记录 →</button>
  </div>
</div>

<!-- 页面过渡遮罩（GSAP 控制黑屏淡入淡出）-->
<div id="page-transition" style="z-index:100;opacity:0;"></div>

<!-- 主界面 -->
<header>品牌 + 统计</header>
<main>
  <aside>左侧面板：城市/省份视图 + 旅行轨迹</aside>
  <section>右侧面板：搜索 + 城市列表</section>
  <div id="chart">ECharts 地图</div>
</main>

<!-- 弹窗 -->
<div id="modalOverlay">城市编辑弹窗</div>
<div id="lightbox">照片预览</div>
<div id="timelineModalOverlay">时间线弹窗</div>
<div id="attDetailOverlay">景点详情</div>
<div id="toast">提示</div>
```

### 3.3 `<script>` 区块

**外部脚本**（`</body>` 前）：
```html
<script src="china-geo.js"></script>           <!-- 中国边界，暴露 window.CHINA_GEO -->
<script src="china-provinces-geo.js"></script> <!-- 34省地级市边界 -->
<script src="https://cdn.jsdelivr.net/npm/globe.gl"></script>      <!-- 3D地球 -->
<script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script> <!-- GSAP动画 -->
```

**GSAP 入场动画**（独立 script 块，GSAP 加载后立即执行）：
```javascript
gsap.fromTo('.launch-title-wrap', {y:40, autoAlpha:0}, {y:0, autoAlpha:1, duration:1.2, ease:'power3.out', delay:0.3});
gsap.fromTo('.launch-subtitle',   {y:30, autoAlpha:0}, {y:0, autoAlpha:1, duration:1.0, ease:'power3.out', delay:0.7});
gsap.fromTo('.launch-btn',        {y:30, autoAlpha:0}, {y:0, autoAlpha:1, duration:1.0, ease:'back.out(1.4)', delay:1.0});
```

**粒子星空背景**（`<script type="module">`，动态 import ogl）：
- 从 `https://esm.sh/ogl@1.0.11` 动态加载 ogl
- 500 个粒子，球体分布，蓝白冷色调（`#ffffff` `#a5d8ff` `#74c0fc`）
- vertex/fragment shader 实现圆形透明粒子 + 正弦扰动
- 鼠标视差：粒子整体平移跟随鼠标（lerp 缓动）
- 渲染循环：requestAnimationFrame 持续渲染

**地球初始化 + GSAP 点击时间线**（普通 script）：
```javascript
function initGlobe(){
  // 等待 globe.gl 加载
  // 初始化地球：纹理 + 凹凸贴图 + 自转 + altitude:1.5
  // GSAP 按钮 hover 动画（paused tween，mouseenter play / mouseleave reverse）
  // GSAP 点击时间线（gsap.timeline）：
  //   步骤1: 按钮按压 scale 0.94→1.0 反馈
  //   步骤2: camProxy tween 驱动镜头缓动拉近中国（lat:36 lng:105 altitude:1.3，1.6s）
  //   步骤3: 停留欣赏中国版图（0.8s）
  //   步骤4: UI 元素 autoAlpha 淡出（0.6s）
  //   步骤5: 黑屏遮罩淡入（0.5s）
  //   步骤6: gsap.set('#launch-screen', {display:'none'}) 隐藏地球层
  //   步骤7: 黑屏遮罩淡出露出主地图（0.6s）
  //   步骤8: call(() => window.dispatchEvent(new Event('resize'))) 触发 ECharts resize
  // 加载 three.js UMD → 云层 + 光照
}
```

**主业务逻辑 IIFE**（约第 3400-7400 行）：
- `PerfScheduler`：全局交互状态调度器（交互期间降帧）
- ECharts 地图初始化（4 重 CDN 回退）
- 城市数据管理（IndexedDB 存储照片 Base64）
- 城市点亮 / 编辑 / 取消点亮
- 照片上传 / 拖拽 / 预览
- 标签管理 / 景点管理
- 旅行轨迹时间线（同年同月按日期排序）
- 省份筛选 / 城市搜索
- ferrofluid 铁磁流体背景动画
- 流星特效
- Variable Proximity 标题动画

---

## 四、外部依赖（CDN 运行时加载）

| 依赖 | 用途 | CDN 来源 |
|------|------|----------|
| Inter 可变字体 | Variable Proximity 标题动画 | fonts.googleapis.com |
| ECharts 5.4.3 | 中国地图渲染/缩放/拖拽 | jsdelivr / bootcdn / unpkg（4 重回退） |
| globe.gl | 3D 云层地球 | cdn.jsdelivr.net/npm/globe.gl |
| GSAP 3.12.5 | 全页面动画引擎（timeline/缓动/GPU加速） | cdn.jsdelivr.net/npm/gsap |
| ogl 1.0.11 | 粒子星空背景 WebGL | esm.sh/ogl（动态 import） |
| three.js 0.149.0 | 云层 Mesh / 光照（UMD） | cdn.jsdelivr.net/npm/three |
| 中国 GeoJSON | 省份/地级市边界 | 本地 china-geo.js / china-provinces-geo.js |

---

## 五、完整修改历史（按时间顺序，共 34 次提交）

以下是从最早到最新的全部提交记录，完整记录了项目演进过程：

### 阶段一：项目初始上传
1. `7bcb27a` - Add files via upload（初始项目上传）
2. `5b58b1b` - Add files via upload
3. `73bd41f` - Add files via upload

### 阶段二：性能与体验优化
4. `340afbe` - perf: 静态资源(js/json/css/图片)启用7天immutable缓存
5. `8dd72ef` - fix: 34省地级市边界本地化，修复城市数从371降级为35的问题
6. `d03b3eb` - feat: 景点照片上传支持拖拽，照片同步存入景点相册和城市主相册
7. `76db29d` - feat: 城市编辑弹窗尺寸放大+布局美化+滚动隔离
8. `1ffd263` - feat: 照片网格区新增"照片管理"标题，与标签管理/景点管理样式统一
9. `73adccd` - style: 统一照片管理/标签管理/景点管理三标题字号、字重、颜色、对齐
10. `d4dfe6b` - fix: 城市封面预览拉伸变形——固定16:7宽高比容器+object-fit:cover
11. `9abb977` - style: 缩小城市编辑弹窗整体尺寸，内部排版紧凑化
12. `804aa2c` - style: 缩小城市封面预览尺寸，限宽280px不再撑满弹窗
13. `81cde10` - style: 城市封面高度固定250px+水平居中(左右空余相同)
14. `44503f6` - perf: 全局性能优化——交互期间降帧+合成层隔离+rect缓存
15. `1b5cd58` - feat: 城市编辑弹窗新增「取消点亮」按钮，一键清空当前城市所有内容
16. `a28710c` - feat: 旅行轨迹时间线排序优化——同年同月多城市按游玩日期细分排序
17. `109e393` - fix: 标签管理输入框白字白底看不见——加深色背景

### 阶段三：3D 地球启动页开发（独立文件 earth-startup.html）
18. `20115f1` - feat: 新增globe.gl云层3D地球启动页(earth-startup.html)
19. `e95315d` - fix: 修复earth-startup.html双击file://打开看不到3D地球的问题
20. `fcec565` - style: 调整启动页地球尺寸——altitude 2.5→3.6 缩小至约视口45%
21. `55e3a6d` - style: 启动页地球放大——altitude 3.6→1.3 接近全屏铺满
22. `175e86b` - style: 启动页标题居中+按钮参考照片风格
23. `faacc03` - feat: 启动页交互完善——全球视角+镜头过渡到中国+去除高光

### 阶段四：动态背景迭代（earth-startup.html）
24. `eff48d7` - feat: 启动页新增reactbits Prism棱镜动态背景
25. `f938269` - fix: 棱镜背景改CSS实现——解决file://不显示+地球拖拽卡顿
26. `a4aaa02` - fix: 修复棱镜光流背景不可见——调整图层位置到地球上方
27. `1fcf7d5` - feat: 棱镜背景改用Canvas2D——流动光线+粒子+极光波动
28. `f2256f2` - feat: 背景替换为reactbits Particles粒子星空
29. `97c9770` - feat: 粒子密度和亮度大幅提升+鼠标交互
30. `2f95943` - style: 调整粒子大小和配色比例
31. `e295e67` - style: 地球尺寸放大——altitude 2.5→1.5

### 阶段五：HyperFrames/GSAP 动画重构
32. `2d1273a` - feat: HyperFrames/GSAP重构全部动画——timeline串联+GPU加速
    - 引入 GSAP 3.12.5，用 gsap.timeline 串联所有动画
    - 入场动画（gsap.fromTo 标题/副标题/按钮）
    - 按钮 hover（GSAP paused tween）
    - 点击时间线（按压→镜头→停留→UI淡出→黑屏→跳转）
    - 新增 #page-transition 过渡遮罩

### 阶段六：合并进主项目 + 最终优化
33. `cb1344e` - feat: 地球启动页合并进主项目index.html——单文件整合+GSAP动画
    - 将 earth-startup.html 完整合并进 index.html
    - #launch-screen 全屏容器 z-index:50 覆盖主内容
    - 点击跳转改为 gsap.set display:none 隐藏地球层
    - 新增中国国界呼吸高光特效（后移除）
34. `e5ecaa2` - perf: 优化粒子互动卡顿——降粒子数+简化鼠标交互
    - particleCount 800→500
    - 移除鼠标旋转偏移，只保留平移视差

---

## 六、关键技术决策与实现细节

### 6.1 3D 地球（globe.gl）
- **为何用 globe.gl**：封装了 three.js，API 简洁，支持 UMD 加载（file:// 可用）
- **云层实现**：globe.gl 不暴露全局 THREE，需额外动态加载 three@0.149.0 UMD 创建云层 Mesh
- **镜头控制**：globe.gl 的 `pointOfView()` 不支持 GSAP 直接 tween，用 camProxy 代理对象 + onUpdate 回调驱动
- **初始视角**：`altitude:1.5` 让地球占满大部分屏幕（值越小地球越大）

### 6.2 粒子星空背景（ogl）
- **为何用 ogl 而非 Canvas 2D**：reactbits Particles 组件基于 ogl 实现 3D 粒子（Points + 透视相机 + 球体分布），Canvas 2D 难以还原
- **ESM 动态 import**：`import('https://esm.sh/ogl@1.0.11')`，file:// 下粒子不显示但地球正常（优雅降级）
- **性能优化**：粒子数从 800 降到 500，移除鼠标旋转偏移，避免和 globe.gl 两个 WebGL context 争抢 GPU

### 6.3 GSAP 动画引擎
- **为何用 GSAP**：GPU 加速 transform/opacity，规避 setTimeout 卡顿，timeline 串联多步骤动画
- **三层动画解耦**：粒子(ogl) / 地球(globe.gl) / UI文字按钮(GSAP) 互不抢占渲染资源
- **入场动画用 fromTo 而非 from**：明确起止状态，避免 CSS opacity:0 冲突
- **autoAlpha 替代 opacity**：同时控制 opacity + visibility，防闪烁

### 6.4 页面切换（非跳转）
- **原方案**：`window.location.href = './index.html'` 跳转到主页面
- **合并后方案**：GSAP 隐藏 `#launch-screen`（display:none）+ 触发 `window.dispatchEvent(new Event('resize'))` 让 ECharts 重新计算尺寸
- **过渡流程**：黑屏淡入 → 隐藏地球层 → 黑屏淡出 → resize

### 6.5 数据存储
- 未登录时照片以 Base64/Data URL 存入 IndexedDB，仅当前设备可见
- 登录后 Supabase 保存旅行快照，私有阿里云 OSS 保存实际照片，IndexedDB 作为本机缓存
- 同一账号可跨设备自动恢复；不同账号通过 RLS、用户目录与 Edge Function 路径校验相互隔离

---

## 七、推送 GitHub 全过程

### 7.1 环境配置
```
Git 路径：C:\Program Files\Git\cmd\git.exe
GitHub CLI 路径：C:\Program Files\GitHub CLI
项目目录：F:\codex\codex-list\projects\Your-China-Travel
远程仓库：https://github.com/zwxll/Your-China-Travel.git
分支：main
```

### 7.2 标准推送命令（PowerShell）
```powershell
$env:PATH='C:\Program Files\Git\cmd;C:\Program Files\Git\bin;C:\Program Files\Git\mingw64\bin;C:\Program Files\GitHub CLI;'+$env:PATH
$git='C:\Program Files\Git\cmd\git.exe'
Set-Location 'F:\codex\codex-list\projects\Your-China-Travel'
& $git add -A
& $git commit -m "feat: 提交信息"
& $git push
```

### 7.3 注意事项
- PowerShell 中用 `;` 分隔命令（非 `&&`）
- 文件路径用单引号（PowerShell 单引号是字面量）
- GitHub 网络偶有超时，push 失败时重试即可
- commit message 用中文，遵循 conventional commits 格式（feat/fix/style/perf）
- 推送后 GitHub Pages 自动构建，1-2 分钟后生效，需强制刷新（Ctrl+Shift+R）

### 7.4 部署
- GitHub Pages：仓库 Settings → Pages → main 分支自动部署
- 也可用 Vercel（vercel.json 已配置）/ Nginx（nginx.conf）/ Docker（Dockerfile + docker-compose.yml）

---

## 八、当前最终状态

### 8.1 最终文件结构
- `index.html`：单文件主页面，包含地球启动层 + 主地图 + 全部业务逻辑
- `earth-startup.html`：独立地球启动页备份（保留，未删除）
- 其他配置文件不变

### 8.2 最终动画效果
1. 页面加载 → 地球启动层全屏覆盖主内容
2. GSAP 入场动画：标题 → 副标题 → 按钮依次浮入
3. 地球自动缓慢自转 + 粒子星空背景流动
4. 鼠标可穿透粒子层拖拽地球 / 滚轮缩放
5. 粒子随鼠标平移产生视差效果
6. 点击「开始记录」→ GSAP timeline：
   - 按钮按压反馈 → 镜头缓动聚焦中国 → 停留 → UI淡出 → 黑屏过渡 → 隐藏地球层 → 黑屏淡出 → 主地图显示
7. 主地图：ECharts 中国地图 + 城市点亮 + 照片上传 + 轨迹时间线等全部功能

### 8.3 关键参数
| 参数 | 值 | 位置 |
|------|-----|------|
| 粒子数 particleCount | 500 | index.html 粒子脚本 |
| 粒子配色 | #ffffff #a5d8ff #74c0fc | 同上 |
| 粒子大小 particleBaseSize | 100 | 同上 |
| 地球初始 altitude | 1.5 | initGlobe |
| 镜头聚焦 altitude | 1.3 | 点击时间线步骤2 |
| GSAP CDN | 3.12.5 | head script |
| globe.gl | latest | head script |

---

## 九、给接手 Agent 的建议

1. **修改 index.html 时注意行数会变化**：文件约 7400 行，修改后行号偏移，用 Grep 定位而非固定行号
2. **地球启动层脚本在主业务 IIFE 之前**：注意脚本加载顺序，globe.gl 和 GSAP 需在 initGlobe 前加载
3. **粒子脚本用 type="module"**：动态 import ogl，与普通 script 隔离
4. **测试时用 http-server**：`npx http-server -p 8782 -c-1 --cors`，file:// 下粒子不显示
5. **浏览器测试用 integrated_browser MCP**：navigate → wait_for → evaluate → take_screenshot
6. **推送前先停 http-server**：避免端口占用
7. **GitHub 偶有网络超时**：push 失败重试即可，commit 不会丢失
8. **earth-startup.html 保留作备份**：不要删除，是独立地球页的完整实现

---

*初始文档生成时间：2026-07-20*
*当前基线更新时间：2026-08-14*
*文档整理前最新提交：9ee1b61*
